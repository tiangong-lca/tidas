- --
title: "Flows"
- --

import FlowsSchema from '@/public/schemas/tidas_flows.json';
import FlowsElementaryCategorySchema from '@/public/schemas/tidas_flows_elementary_category.json';
import FlowsProductCategorySchema from '@/public/schemas/tidas_flows_product_category.json';
import { JsonSchemaViewer } from '@/components/json-schema-viewer';

## JSON Schema for Flow and Flow categories

<Callout type="idea">
Flow datasets contain invariant information about substances or energy forms, aligned with the "inputs and outputs" section of ISO/TS 14048. Each flow represents a physical exchange (e.g., material, energy, or waste), including identifiers and classifications. Flows are the building blocks of process and system datasets, ensuring consistency and traceability.
</Callout>

<JsonSchemaViewer schema={ FlowsSchema } />

## Elementary Flow category

<JsonSchemaViewer schema={ FlowsElementaryCategorySchema } />

## Product / Waste Flow category

<JsonSchemaViewer schema={ FlowsProductCategorySchema } />
